# SameVault workspace tmux config
# Hides status bar, optimized for terminal-in-browser.

set -g status off
set -g default-terminal "xterm-256color"
set -g escape-time 0
set -g history-limit 10000
set -g mouse on
